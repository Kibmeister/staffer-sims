# Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Environment
ENVIRONMENT=development
DEBUG=true

# API Provider Configuration
# Options: "openai", "openrouter", or "both"
# - "openai": Use OpenAI for both SUT and Proxy
# - "openrouter": Use OpenRouter for both SUT and Proxy  
# - "both": Use OpenAI for SUT, OpenRouter for Proxy (default)
API_PROVIDER=openrouter

# API Keys (REQUIRED based on API_PROVIDER setting)
# If API_PROVIDER=openai: Only OPENAI_API_KEY required
# If API_PROVIDER=openrouter: Only OPENROUTER_API_KEY required  
# If API_PROVIDER=both: Both keys required
OPENAI_API_KEY=your_openai_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key_here
LANGFUSE_SECRET_KEY=your_langfuse_secret_key_here

# Service URLs
SUT_URL=http://localhost:8080/sut/chat
PROXY_URL=https://openrouter.ai/api/v1/chat/completions
LANGFUSE_HOST=https://cloud.langfuse.com

# Performance Settings
MAX_TURNS=18
REQUEST_TIMEOUT=30  # Individual API request timeout (seconds)
RETRY_ATTEMPTS=3
RETRY_DELAY=1.0

# File Paths
OUTPUT_DIR=output
CONFIG_DIR=config

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
